<div class="docs-header container center-content">
    <h1>Documentation</h1>
    <div class="docs">
        {{{ gettingStarted }}}
        <hr>
        {{{ configurations }}}
        <hr>
        {{{ roleReactions }}}
        <hr>
        {{{ spotifyPlugin }}}
        <hr>
        {{{ webhooks }}}
        <hr>
        <h2>Commands</h2>
        {{#each commands }}
            {{{ this }}}
        {{/each}}
        <hr>
        {{{ gettingHelp }}}
    </div>
</div>
